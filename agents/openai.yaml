interface:
  display_name: "TRACK · 트랙"
  short_description: "목표를 지키며 복잡한 작업을 검증된 완료까지 조율합니다"
  default_prompt: "Use $track to orchestrate this complex work around one stable goal and follow it to a verified finish."
